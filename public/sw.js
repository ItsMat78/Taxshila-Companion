
// DO NOT EDIT THIS FILE. It is automatically generated and managed.
// For more information, see https://firebase.google.com/docs/web/setup#available-libraries

// Import and configure the Firebase SDK
// These scripts are over 100KB - consider hosting them locally using npm
importScripts('https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js');
importScripts('https://www.gstatic.com/firebasejs/9.22.0/firebase-messaging-compat.js');

// This is the "Default" Firebase config object from the Firebase console
const firebaseConfig = {
    apiKey: process.env.NEXT_PUBLIC_FIREBASE_API_KEY,
    authDomain: process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN,
    projectId: process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID,
    storageBucket: process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET,
    messagingSenderId: process.env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID,
    appId: process.env.NEXT_PUBLIC_FIREBASE_APP_ID,
};

firebase.initializeApp(firebaseConfig);

const messaging = firebase.messaging();

// To be able to receive notifications while the app is in the background,
// you need to add a "background" message handler.
// For more information, see https://firebase.google.com/docs/cloud-messaging/js/receive
messaging.onBackgroundMessage((payload) => {
  console.log(
    '[sw.js] Received background message: ',
    payload
  );
  
  const notificationTitle = payload.notification.title;
  const notificationOptions = {
    body: payload.notification.body,
    icon: payload.notification.icon || '/logo.png',
  };

  self.registration.showNotification(notificationTitle, notificationOptions);
});
