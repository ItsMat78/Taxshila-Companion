
// This service worker file is intentionally left blank.
// The Firebase SDK will handle the service worker logic when initialized in the client.
// This file's presence at the root is what's necessary for the SDK to register correctly.
self.addEventListener('fetch', function(event) {
  // This is a placeholder to ensure the file is treated as a service worker.
});
